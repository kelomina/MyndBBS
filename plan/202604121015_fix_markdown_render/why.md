# 修复 Markdown 渲染问题

## 背景与目的
当前 `frontend` 中的帖子内容 (`post.content`) 是直接以纯文本形式渲染的，这导致包含了 Markdown 语法的帖子（如包含列表、粗体、代码块等）无法正确显示其格式。

## 目标与价值
通过引入 `react-markdown`，使用户发表的包含 Markdown 格式的帖子能够被正确解析并渲染出相应的样式，提升社区用户的阅读体验。