# Backend plugins

Place only reviewed, allow-listed plugin packages here. Each plugin must include
`manifest.json` and an entry file whose SHA-256 matches the manifest. Plugins are
not loaded until the isolated plugin host is configured for the deployment.
