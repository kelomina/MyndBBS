# 任务清单

- [√] 任务 1: 修改 `packages/frontend/src/app/p/[id]/page.tsx`，将传入 `<ReactMarkdown>` 的 `post.content` 处理为 `post.content.replace(/\\n/g, '\n')`。
- [√] 任务 2: 按照 `Smart Function Crafting & Annotation` 要求，记录本次修复步骤。由于不涉及新的 JS/TS 函数，只需修改 `page.tsx` 中已经标注过的函数实现即可。